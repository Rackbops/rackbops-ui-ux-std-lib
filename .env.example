# Box-side only -- copy to `.env` in the stack dir on nucbox, never commit the real value.
# Cloudflare Tunnel token for the rackbops-ui-ux-std-lib tunnel (created via the Cloudflare API,
# not the dashboard). See deploy/README.md.
CLOUDFLARE_TUNNEL_TOKEN=<PLACEHOLDER>
