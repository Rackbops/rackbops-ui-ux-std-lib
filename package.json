{
  "name": "rackbops-ui-ux-std-lib",
  "private": true,
  "packageManager": "pnpm@11.15.1",
  "description": "UX/design-standards library for roshne's apps: CSS themes + React components, one --rb-* contract with swappable themes.",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test && node --test site/serve.test.mjs scripts/generate-skill-table.test.mjs scripts/copy-license.test.mjs",
    "showcase": "node site/serve.mjs",
    "visual": "node scripts/visual.mjs"
  },
  "devDependencies": {
    "lightningcss": "^1.33.0",
    "pixelmatch": "^7.2.0",
    "playwright-core": "1.63.0",
    "pngjs": "^7.0.0"
  }
}
